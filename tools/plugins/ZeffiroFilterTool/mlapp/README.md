# Filter tool — App Designer layout

`zeffiro_interface_filter_tool.mlapp` is the window source. `m/zef_filter_tool.m` constructs it and assigns Add / Apply callbacks. **Add** parses `Description:` / `Input:` / `Output:` from `m/filter_bank/` help — do not remove those tags from filter-bank files.

Parent: [../README.md](../README.md). Filter-bank stages: [../m/filter_bank/README.md](../m/filter_bank/README.md).
