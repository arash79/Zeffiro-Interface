# Find synthetic gravity data — GUIDE figure

`find_synthetic_gravity_data.fig` is opened by `m/zef_find_synthetic_gravity_data.m`. Title: **ZEFFIRO Interface: Find Synthetic Gravity Data**. Compute/plot callbacks live in the fig; the algorithms they run are `zef_synthetic_gravity_data` and `zef_plot_gravity_roi`. Parent: [../README.md](../README.md).
