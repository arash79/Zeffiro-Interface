# Preconditioned relaxation — App Designer layout

`zef_relax.mlapp` is the window source. `m/zef_relax_inversion_tool` opens it; the two action buttons run `zef_relax_iteration` and `zef_relax_find_preconditioner`. Parent: [../README.md](../README.md).
