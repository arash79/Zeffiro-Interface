# HBSampler / fig

GUIDE layout `hb_sampler.fig` is a leftover. Live `hb_sampler.m` opens `zef_mcmc_window` (not this fig). Algorithm and usage are in `m/` and the parent README.

The Start string saved in this fig (`mcmc_sampler`) is not the live callback.
