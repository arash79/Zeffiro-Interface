# RAMUSSampler / fig

GUIDE layout for RAMUSSampler (`ramus_sampler.fig`). Algorithm and usage are in `m/` and the parent README.

Start Callback is stored in this figure (`ramus_sampling_process`). `m/ramus_sampler.m` only `open`s the fig and runs `zef_init_ramus_sampler`.
