# Find synthetic source — App Designer layout

`find_synthetic_source_app.mlapp` is the non-legacy window. Start / callbacks: `m/find_synthetic_source.m`. The legacy GUIDE tool is a separate plugin (`FindSyntheticSourceLegacy`). Parent: [../README.md](../README.md).
