# Dipole Scan — App Designer layout

`dipole_app.mlapp` is the window source. Start: plugin `zef_dipole_start` (Inverse tools → Dipole Scan). Solver is `m/zef_dipoleScan`, not `inverse.DipoleScanInverter`. Parent: [../README.md](../README.md).
