# `scripts/data`

One maintainer `.mat` next to the Python helpers: `SEP_synth_source_data.mat`. It is **not** referenced from first-party MATLAB in this tree (no `load` callers). Treat it as a leftover synthetic-source fixture, not runtime input.

Not on the MATLAB path. Parent: [`scripts/README.md`](../README.md).
